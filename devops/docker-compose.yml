services:
    frontend-qa:
        build:
            context: ..
            dockerfile: devops/Dockerfile
        container_name: frontend-qa
        restart: unless-stopped
        networks:
            - qa_network

networks:
    qa_network:
        external: true
